# Planning / personal files — kept locally, not part of the published repo
DOCUMENTATION.md
plan.md
process.md
*.jpg
*.jpeg
*.png
*.gif
*.heic
*.webp

# ...except the assets the published site actually serves. The résumé is
# linked from the page, so it has to ship — note it contains a phone number.
!portrait.jpg
!portrait.webp
# ...and the responsive variants the hero <picture>/preload srcsets point at.
# Without these the srcset candidates 404 in production and the LCP image
# never paints.
!portrait-400.jpg
!portrait-400.webp
!portrait-600.jpg
!portrait-600.webp
!Mohankumar-Marimuthu-Resume.pdf

# Secrets — never commit
.env
.env.*
wrangler.toml
.dev.vars
.wrangler/

# OS / editor cruft
.DS_Store
Thumbs.db
